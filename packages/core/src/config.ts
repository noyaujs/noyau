import type { NoyauConfig } from "@noyau/schema";
import type { DefineConfig, ConfigLayerMeta } from "c12";

export const defineNoyauConfig: DefineConfig<NoyauConfig, ConfigLayerMeta> = (
  config
) => config;
