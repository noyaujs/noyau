{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["./dist/**"]
    },
    "build:stub": {
      "dependsOn": ["^build:stub"],
      "outputs": ["./dist/**"]
    }
  }
}
